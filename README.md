# TTM — Система управления задачами «Транстелематика»

Корпоративное Django-приложение для компании «Транстелематика»: управление задачами, планирование, оценка рисков, дашборды, роли, AI-помощник (Gemini) и генерация отчётов.

---

## Быстрый старт (Windows)

### 1. Клонировать репозиторий

```powershell
git clone <URL-репозитория>
cd <имя-папки>
```

### 2. Создать виртуальное окружение и активировать его

```powershell
python -m venv .venv
.\.venv\Scripts\activate
```

### 3. Установить зависимости

```powershell
pip install -r requirements.txt
```

### 4. Применить миграции и загрузить демо-данные

В проекте уже есть готовая база данных `db.sqlite3` с демо-данными. Если вы хотите пересоздать её с нуля:

```powershell
del db.sqlite3
python manage.py migrate
python manage.py seed_data
```

Если вы хотите использовать готовую базу — **просто пропустите этот шаг**, база уже включена в репозиторий.

### 5. Запустить сервер

```powershell
python manage.py runserver
```

Откройте в браузере: **http://127.0.0.1:8000/**

---

## Учётные записи для входа

| Логин      | Пароль       | Роль                          |
|------------|--------------|-------------------------------|
| `ivan`     | `demo1234`   | Руководитель команды          |
| `natalia`  | `demo1234`   | Руководитель подразделения    |
| `admin`    | `admin1234`  | Администратор (доступ `/admin/`) |
| `maria`    | `demo1234`   | Сотрудник (АХО)               |
| `alexey`   | `demo1234`   | Сотрудник (АХО)               |
| `olga`     | `demo1234`   | Руководитель команды (HR)     |
| `anna`     | `demo1234`   | Сотрудник (HR)                |
| `dmitry`   | `demo1234`   | Сотрудник (ИТ)                |
| `elena`    | `demo1234`   | Сотрудник (Финансы)           |
| `sergey`   | `demo1234`   | Сотрудник (Юридический отдел) |

---

## Конфигурация (.env)

Файл `.env` уже включён в репозиторий с рабочими настройками. Дополнительная настройка **не требуется**.

Ключевые переменные:

| Переменная        | Значение по умолчанию                      | Описание                                                  |
|-------------------|--------------------------------------------|-----------------------------------------------------------|
| `SECRET_KEY`      | `django-insecure-ttm-demo-only-...`        | Ключ Django (для демо, не для продакшена)                 |
| `DEBUG`           | `true`                                     | Режим отладки                                             |
| `DATABASE_URL`    | `postgresql://...`                         | URL базы данных (игнорируется — используется SQLite)      |
| `GEMINI_API_KEY`  | *(ключ включён)*                           | API-ключ для Google Gemini AI                             |
| `AI_MOCK_MODE`    | `false`                                    | `false` — реальный AI, `true` — заглушка                  |

> **Примечание:** Если PostgreSQL **не установлен**, приложение автоматически использует встроенный SQLite. Никакой дополнительной настройки БД не нужно.

---

## Структура проекта

```
TTM/
├── apps/                        # Приложения Django
│   ├── accounts/                # Профили пользователей и роли
│   ├── ai_assistant/            # AI-помощник (Gemini)
│   ├── audit/                   # Журнал аудита
│   ├── dashboards/              # Дашборды и аналитика
│   ├── departments/             # Подразделения и команды
│   ├── notifications/           # Уведомления
│   ├── reports/                 # Генерация отчётов (PDF/Excel)
│   └── tasks/                   # Управление задачами (Канбан)
├── templates/                   # HTML-шаблоны
├── static/                      # CSS, JS, изображения
├── ttm_project/                 # Настройки Django проекта
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── .env                         # Переменные окружения (включён)
├── db.sqlite3                   # Готовая база с демо-данными (включена)
├── manage.py
└── requirements.txt             # Зависимости Python
```

---

## Основные функции

- **Канбан-доска задач** — управление задачами с drag-and-drop, фильтрация и поиск
- **Иерархия задач** — годовые → квартальные → месячные → недельные цели
- **Автоматическая оценка рисков** — на основе дедлайнов, прогресса и нагрузки
- **AI-помощник (Gemini)** — анализ задач, рекомендации, генерация описаний
- **Дашборды** — статистика, графики, сводки по подразделениям
- **Отчёты** — генерация PDF и Excel отчётов
- **Уведомления** — оповещения о рисках, дедлайнах и изменениях
- **Журнал аудита** — полная история всех действий
- **Ролевая модель** — сотрудник, руководитель команды, руководитель подразделения, администратор

---

## Проверка работоспособности

```powershell
python manage.py check
python manage.py test
```

---

## Технологии

- **Python 3.12+**
- **Django 5.2**
- **SQLite** (для разработки и демо)
- **Google Gemini AI** (генеративный AI-помощник)
- **ReportLab / OpenPyXL** (PDF и Excel отчёты)
